[package]
name = "aios-intent"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rust-for-Linux telemetry boundary for the AIOS local runtime"

[lib]
crate-type = ["staticlib"]

[features]
default = []
rust-for-linux = []
