# AIOS — Core Dependencies (covers v1, v2, and v3)
# Run: pip install -r requirements.txt

# ── Qt UI Framework ───────────────────────────────────────────────
PySide6>=6.6.0

# ── LLM Backends ─────────────────────────────────────────────────
# Ollama SDK (recommended, easiest setup)
ollama>=0.4.0

# llama.cpp Python bindings (optional — for direct GGUF inference)
# Uncomment and install for llama.cpp support:
# llama-cpp-python>=0.2.0

# ── RAG / Embeddings / Vector Search ─────────────────────────────
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
numpy>=1.24.0

# ── Web Search + Content Fetching ────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── System Monitoring ────────────────────────────────────────────
psutil>=5.9.0

# ── Optional: Screenshot + Window Control ────────────────────────
# Uncomment if you want computer control tools:
# pillow>=10.0.0
# pygetwindow>=0.0.9   # Windows only

# ── Optional: GPU Profiling ──────────────────────────────────────
# pyaccelerate>=0.20.0
